# File Server

```
Usage: file-server-rs.exe [OPTIONS] --root <ROOT>

Options:
  -r, --root <ROOT>  Root directory of the files to serve
  -a, --addr <ADDR>  Desired IP address of the server [default: 0.0.0.0:8080]
  -h, --help         Print help
  -V, --version      Print version
```